/* 
 * POORNIMA ATTENDANCE SYSTEM - Environment Configuration (Example)
 * 
 * Instructions:
 * 1. Copy this file and rename it to `env.js`.
 * 2. `env.js` is ignored by git to prevent leaking secrets.
 * 3. Replace the placeholder values with your actual API keys.
 */

window.ENV = {
  // Google Calendar API integration
  GOOGLE_CALENDAR_API_KEY: '',
  GOOGLE_CALENDAR_ID: 'en.indian#holiday@group.v.calendar.google.com' // Default: Indian Holidays
};
